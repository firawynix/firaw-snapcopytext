services:
  site:
    image: nginx:alpine
    container_name: snapcopytext-site
    restart: unless-stopped
    ports:
      # Só loopback: quem publica é o Cloudflare Tunnel do mesmo servidor.
      - "127.0.0.1:26005:80"
    volumes:
      - ./public:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    read_only: true
    tmpfs:
      - /var/cache/nginx
      - /var/run
    security_opt:
      - no-new-privileges:true
