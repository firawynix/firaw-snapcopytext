<Window x:Class="Firaw.SnapCopyText.Views.TextPromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Firaw - Adicionar texto" Width="470" Height="245"
        ResizeMode="NoResize" WindowStartupLocation="CenterOwner" ShowInTaskbar="False">
    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Digite o texto da anotação" FontSize="18" FontWeight="SemiBold" />
        <TextBox x:Name="InputText" Grid.Row="1" Margin="0,12" Padding="11,8"
                 AcceptsReturn="True" TextWrapping="Wrap" FontSize="15"
                 Background="{StaticResource FirawSurface}" Foreground="{StaticResource FirawText}"
                 BorderBrush="{StaticResource FirawCyan}" SelectionBrush="{StaticResource FirawCyan}" />
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancelar" Style="{StaticResource FirawSecondaryButton}"
                    Margin="0,0,8,0" Click="CancelButton_Click" />
            <Button Content="Adicionar" Style="{StaticResource FirawButton}" Click="AddButton_Click" />
        </StackPanel>
    </Grid>
</Window>
